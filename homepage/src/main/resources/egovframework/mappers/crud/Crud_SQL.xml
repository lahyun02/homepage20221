<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.let.crud.service.impl.CrudMapper">

	<resultMap id="temp" type="egovframework.let.crud.service.CrudVO">
		<result property="crudId" column="CRUD_ID" />
		<result property="crudSj" column="CRUD_SJ" />
		<result property="crudCn" column="CRUD_CN" />
		<result property="userNm" column="USER_NM" />
		<result property="registDate" column="FRST_REGIST_PNTTM" />
		<result property="dateFormat" column="DATE_FORMAT" />
		
	</resultMap>
	
	<select id="selectTemp" resultType="egovframework.let.crud.service.CrudVO">
		SELECT
			CRUD_ID AS CrudId
			, CRUD_SJ AS CrudSj
			, CRUD_CN AS CrudCn
			, USER_NM AS UserNm
			, FRST_REGIST_PNTTM AS RegistDate
		FROM LETCRUD
		WHERE CRUD_ID = #{crudId}
	</select>
	
	
	<select id="selectTempList" resultType="egovMap">
		SELECT
			CRUD_ID
			, CRUD_SJ
			, CRUD_CN
			, USER_NM
			, FRST_REGIST_PNTTM AS REGIST_DATE
			<!-- DATE_FORMAT(NOW(), '%Y-%m-%d') AS registDate -->
		FROM LETCRUD
		ORDER BY CRUD_ID DESC
		LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
		
	</select>
	
	
	<insert id="insertTemp" parameterType="egovframework.let.crud.service.CrudVO"> 
		INSERT INTO LETCRUD (
			CRUD_ID
			, CRUD_SJ
			, CRUD_CN
			, USER_NM
			, FRST_REGIST_PNTTM
		) VALUES (
			#{crudId}
			, #{crudSj}
			, #{crudCn}
			, #{userNm}
			, now()
		)
	</insert>
	
	<update id="updateTemp" parameterType="egovframework.let.crud.service.CrudVO">
		UPDATE LETCRUD SET
			CRUD_SJ = #{crudSj}
			, CRUD_CN = #{crudCn}
			, USER_NM = #{userNm}
		WHERE CRUD_ID = #{crudId}
	</update>
	
	<delete id="deleteTemp" parameterType="egovframework.let.crud.service.CrudVO">
		DELETE FROM LETCRUD
		WHERE CRUD_ID = #{crudId}
	</delete>
	
	<!-- 총 페이징수 -->
	<select id="selectTempListCnt" resultType="java.lang.Integer">
		SELECT
			COUNT(*) CNT
		FROM LETCRUD	
	</select>
	
	
	

</mapper>